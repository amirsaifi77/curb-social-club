// Placeholder entry point wired in session 0.1 so the workspace pipeline runs.
// Session 0.9 adds src/generated.d.ts (openapi-typescript output, committed,
// never hand-edited) and re-exports paths, components, and friendly aliases.
export const packageName = '@curb/types';
