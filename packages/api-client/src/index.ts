// Placeholder entry point wired in session 0.1 so the workspace pipeline runs.
// createClient, ApiError, query keys, and TanStack Query hooks land once
// @curb/types carries the generated OpenAPI types (session 0.9 onward).
export const packageName = '@curb/api-client';
