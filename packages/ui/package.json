{
  "name": "@curb/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "@curb/config": "workspace:*",
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.1.13",
    "eslint": "^9.17.0",
    "jsdom": "^27.0.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "description": "Shared logic and headless hooks for web and mobile; no rendering code.",
  "dependencies": {
    "@curb/design-tokens": "workspace:*"
  },
  "peerDependencies": {
    "react": ">=18"
  }
}
