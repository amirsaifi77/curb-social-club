// Placeholder entry point wired in session 0.1 so the workspace pipeline runs.
// Session 0.3 replaces this with the token build (themes, getTheme, typography,
// spacing, radius, glass) generated from ../tokens.json per the README.
export const packageName = '@curb/design-tokens';
